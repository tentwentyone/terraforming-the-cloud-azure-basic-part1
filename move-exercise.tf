# ## 3.3 Descomentar apenas quando for pedido.

# resource "random_pet" "new_id" {
#   length    = 2
#   separator = "-"
#   prefix    = var.prefix
# }

# moved {
#   from = random_pet.new_id
#   to = random_pet.moved
# }
